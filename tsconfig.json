{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" }, //2 separate tsconfig as there's 2 environment, the app(browser)
    { "path": "./tsconfig.node.json" } //and the node, vite
  ]
}
